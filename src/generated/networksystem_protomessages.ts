// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v4.25.9
// source: networksystem_protomessages.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export interface NetMessageSplitscreenUserChanged {
  slot?: number | undefined;
}

export interface NetMessageConnectionClosed {
  reason?: number | undefined;
  message?: string | undefined;
}

export interface NetMessageConnectionCrashed {
  reason?: number | undefined;
  message?: string | undefined;
}

export interface NetMessagePacketStart {
}

export interface NetMessagePacketEnd {
}

function createBaseNetMessageSplitscreenUserChanged(): NetMessageSplitscreenUserChanged {
  return { slot: 0 };
}

export const NetMessageSplitscreenUserChanged: MessageFns<NetMessageSplitscreenUserChanged> = {
  encode(message: NetMessageSplitscreenUserChanged, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.slot !== undefined && message.slot !== 0) {
      writer.uint32(8).uint32(message.slot);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NetMessageSplitscreenUserChanged {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNetMessageSplitscreenUserChanged();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.slot = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseNetMessageConnectionClosed(): NetMessageConnectionClosed {
  return { reason: 0, message: "" };
}

export const NetMessageConnectionClosed: MessageFns<NetMessageConnectionClosed> = {
  encode(message: NetMessageConnectionClosed, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reason !== undefined && message.reason !== 0) {
      writer.uint32(8).uint32(message.reason);
    }
    if (message.message !== undefined && message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NetMessageConnectionClosed {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNetMessageConnectionClosed();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.reason = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseNetMessageConnectionCrashed(): NetMessageConnectionCrashed {
  return { reason: 0, message: "" };
}

export const NetMessageConnectionCrashed: MessageFns<NetMessageConnectionCrashed> = {
  encode(message: NetMessageConnectionCrashed, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reason !== undefined && message.reason !== 0) {
      writer.uint32(8).uint32(message.reason);
    }
    if (message.message !== undefined && message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NetMessageConnectionCrashed {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNetMessageConnectionCrashed();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.reason = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseNetMessagePacketStart(): NetMessagePacketStart {
  return {};
}

export const NetMessagePacketStart: MessageFns<NetMessagePacketStart> = {
  encode(_: NetMessagePacketStart, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NetMessagePacketStart {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNetMessagePacketStart();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseNetMessagePacketEnd(): NetMessagePacketEnd {
  return {};
}

export const NetMessagePacketEnd: MessageFns<NetMessagePacketEnd> = {
  encode(_: NetMessagePacketEnd, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NetMessagePacketEnd {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNetMessagePacketEnd();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}
